services:
  backend:
    build: ./backend
    container_name: kfokam48-backend
    ports:
      - "8080:8080"
    environment:
      SPRING_PROFILES_ACTIVE: dev

  frontend:
    build: ./frontend
    container_name: kfokam48-frontend
    ports:
      - "5173:80"
    depends_on:
      - backend